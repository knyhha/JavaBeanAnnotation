# JavaBeanAnnotation

A small learning project: a `@JavaBean` annotation and an annotation processor
that generates a JavaBean class from an existing one at compile time.

I learned how to create a custom Java annotation and process it at compile time with an annotation processor that generates code.

## Build

```
mvn clean compile
```

## Ideas

- `equals`, `hashCode`, `toString`
- annotation parameters 
